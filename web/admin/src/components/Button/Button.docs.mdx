import { Story, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from './index.js';

# Button Types

There are 3 types of buttons:

- Default: main button with a label, used for Save, Continue, etc..
- Action: it is an icon button for a main action such as Add new Item
- IconOnly: which is used for secondary actions inside tables for example

# Preview

<Story id="button--default" />

# Props

<ArgsTable of={Button} />
